# CCS Comments
CCS comments are similar to that of Java, just use double slashes like so: `//`

```
// module declaration
def module foo
def desc "Foo's description"

// module contents...
```